# Sample .travis.yml for R projects

language: r

r:
  - oldrel
  - release
  - devel

warnings_are_errors: true
sudo: false
cache: packages

notifications:
  email:
    on_success: true
    on_failure: true

